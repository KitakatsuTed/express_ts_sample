<h1>ホーム</h1>

<% if(!currentUser) { %>
  <a href="/login" class="btn btn-primary">ログイン</a>
  <a href="/sign_up" class="btn btn-primary">サインアップ</a>
<% } %>

<% if(currentUser) { %>
  <a href="/dashboard">ダッシュボードへ</a>
<% } %>
